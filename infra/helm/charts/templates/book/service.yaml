apiVersion: v1
kind: Service
metadata:
  name: book
  namespace: {{ .Values.global.namespace }}
spec:
  ports:
    - port: {{ .Values.book.service.port }}
      targetPort: {{ .Values.book.service.port }}
  selector:
    app: book