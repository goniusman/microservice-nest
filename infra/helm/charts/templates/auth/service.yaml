apiVersion: v1
kind: Service
metadata:
  name: auth
  namespace: {{ .Values.global.namespace }}
spec:
  ports:
    - port: {{ .Values.auth.service.port }}
      targetPort: {{ .Values.auth.service.port }}
  selector:
    app: auth