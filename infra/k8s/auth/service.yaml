apiVersion: v1
kind: Service
metadata:
  name: auth
  namespace: bookverse
spec:
  selector:
    app: auth
  ports:
    - port: 3001
      targetPort: 3001
  type: ClusterIP
  # type: NodePort # <-- Change this from ClusterIP # minikube service auth -n bookverse