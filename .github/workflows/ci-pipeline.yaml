name: Monorepo CI/CD (GitOps Safe)

on:
  push:
    branches: [main]
    paths:
      - 'infra/helm/charts/templates/**'
      - 'services/**'
      - '.github/workflows/ci-pipeline.yaml'

concurrency:
  group: monorepo-ci-cd
  cancel-in-progress: true

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        service:
          - gateway
          - auth
          - book
          - order
          - notification

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Check if service changed
        id: filter
        uses: dorny/paths-filter@v4
        with:
          filters: |
            changed: '${{ matrix.service }}/**'

      - name: Skip if not changed
        if: steps.filter.outputs.changed != 'true'
        run: echo "No changes for ${{ matrix.service }}"

      - name: Log in to Docker Hub
        if: steps.filter.outputs.changed == 'true'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Image
        if: steps.filter.outputs.changed == 'true'
        uses: docker/build-push-action@v6
        with:
          context: ./${{ matrix.service }}
          file: ./${{ matrix.service }}/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/${{ matrix.service }}:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/${{ matrix.service }}:${{ github.sha }}

  gitops-update:
    needs: build-and-push
    runs-on: ubuntu-latest

    steps:
      - name: Checkout GitOps Repo
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/microservice-nest
          token: ${{ secrets.GITOPS_PAT }}
          path: gitops

      - name: Setup yq
        run: |
          sudo wget https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64 -O /usr/bin/yq
          sudo chmod +x /usr/bin/yq

      - name: Update Helm values (safe, no sed)
        run: |
          cd gitops/infra/helm/charts

          # Update all services using SHA tag
          yq e '.gateway.tag = "${{ github.sha }}"' -i values.yaml
          yq e '.auth.tag = "${{ github.sha }}"' -i values.yaml
          yq e '.book.tag = "${{ github.sha }}"' -i values.yaml
          yq e '.order.tag = "${{ github.sha }}"' -i values.yaml
          yq e '.notification.tag = "${{ github.sha }}"' -i values.yaml

      - name: Create Pull Request (NO direct push → avoids loop)
        uses: peter-evans/create-pull-request@v6
        with:
          path: gitops
          branch: gitops/update-${{ github.sha }}
          base: main
          title: "chore: update service images to ${{ github.sha }}"
          commit-message: "chore(cd): update images to ${{ github.sha }} [skip ci]"
          body: |
            Automated GitOps update from CI pipeline.
            This PR updates all microservices to latest build SHA.