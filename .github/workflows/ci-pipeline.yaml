name: Monorepo CI/CD Pipeline

on:
  push:
    branches:
      - main
    paths:
      - 'gateway/**'
      - 'services/**'
      - '.github/workflows/ci-pipeline.yaml' # Triggers when updating this pipeline file itself

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      gateway: ${{ steps.filter.outputs.gateway }}
      auth: ${{ steps.filter.outputs.auth }}
      book: ${{ steps.filter.outputs.book }}
      order: ${{ steps.filter.outputs.order }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Paths Filter Engine
        id: filter
        uses: dorny/paths-filter@v4
        with:
          filters: |
            gateway: 'gateway/**'
            auth: 'services/auth/**'
            book: 'services/book/**'
            order: 'services/order/**'

  build-and-deploy:
    needs: detect-changes
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - service_name: gateway
            context_path: ./services/gateway
            file_path: ./services/gateway/Dockerfile
            enabled: ${{ needs.detect-changes.outputs.gateway }}
          - service_name: auth
            context_path: ./services/auth
            file_path: ./services/auth/Dockerfile
            enabled: ${{ needs.detect-changes.outputs.auth }}
          - service_name: book
            context_path: ./services/book
            file_path: ./services/book/Dockerfile
            enabled: ${{ needs.detect-changes.outputs.book }}
          - service_name: order
            context_path: ./services/order
            file_path: ./services/order/Dockerfile
            enabled: ${{ needs.detect-changes.outputs.order }}
    steps:
      # If this specific service did not change, exit cleanly without error
      - name: Check if Execution is Required
        if: ${{ matrix.enabled != 'true' }}
        run: |
          echo "No changes for ${{ matrix.service_name }}. Skipping."
          exit 0

      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set Short SHA Output
        id: vars
        run: echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Build and Push Container Image
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context_path }}
          file: ${{ matrix.context_path }}/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/${{ matrix.service_name }}:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/${{ matrix.service_name }}:sha-${{ steps.vars.outputs.sha_short }}

      - name: Checkout GitOps Deployment Repo
        uses: actions/checkout@v4
        with:
          repository: '${{ github.repository_owner }}/microservice-nest'
          token: ${{ secrets.GITOPS_PAT }}
          path: gitops-repo

      - name: Write New Target Tag to values.yaml
        run: |
          cd gitops-repo/infra/helm/charts
          
          # Find the block starting with your service name, find the next 'tag:' statement, and overwrite it safely
          sed -i "/^${{ matrix.service_name }}:/,/^[^ ]/ s/tag: .*/tag: \"sha-${{ steps.vars.outputs.sha_short }}\"/" values.yaml
          
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          git add values.yaml
          git commit -m "chore(cd): auto-bump ${{ matrix.service_name }} image to sha-${{ steps.vars.outputs.sha_short }}"
          git push













    # steps:
    #   # FIXED: Removed the dangerous step that used 'exit 0' to abort the container

    #   - name: Checkout Source Code
    #     if: ${{ matrix.enabled == 'true' }}
    #     uses: actions/checkout@v4

    #   - name: Log in to Docker Hub
    #     if: ${{ matrix.enabled == 'true' }}
    #     uses: docker/login-action@v3
    #     with:
    #       username: ${{ secrets.DOCKERHUB_USERNAME }}
    #       password: ${{ secrets.DOCKERHUB_TOKEN }}

    #   - name: Build and Push Container Image
    #     if: ${{ matrix.enabled == 'true' }}
    #     uses: docker/build-push-action@v5
    #     with:
    #       context: ./services/${{ matrix.service_name }}
    #       file: ./services/${{ matrix.service_name }}/Dockerfile
    #       push: true
    #       tags: ${{ secrets.DOCKERHUB_USERNAME }}/${{ matrix.service_name }}:sha-${{ github.sha }}

    #   - name: Checkout GitOps Deployment Repo
    #     if: ${{ matrix.enabled == 'true' }}
    #     uses: actions/checkout@v4
    #     with:
    #       repository: 'goniusman/microservice-nest' 
    #       token: ${{ secrets.GITOPS_PAT }}
    #       path: gitops-repo

    #   - name: Update Service Tag in Helm values.yaml
    #     if: ${{ matrix.enabled == 'true' }}
    #     run: |
    #       cd gitops-repo/infra/helm/charts
          
    #       # Variables cleanly isolated with single quotes for local execution safely
    #       sed -i '/^'${{ matrix.service_name }}':/,/^  [^ ]/ s/tag:.*/tag: "sha-'${{ github.sha }}'"/' values.yaml
          
    #       git config --global user.name "github-actions[bot]"
    #       git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
    #       git add values.yaml
    #       git commit -m "chore(cd): auto-update ${{ matrix.service_name }} image tag to sha-${{ github.sha }}"
    #       git push
